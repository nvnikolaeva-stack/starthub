# Скопируй в .env для локальной разработки (файл .env не коммитится).
DATABASE_URL=sqlite:///./starthub.db
# JSON-массив строк — URL фронтенда (Vercel) и локальный dev.
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
